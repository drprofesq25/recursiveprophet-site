{
  "name": "recursiveprophet-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:prod": "npm run check:placeholders && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:placeholders": "node scripts/check-placeholders.mjs",
    "audit": "astro build && node scripts/audit.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "axe-core": "^4.10.0",
    "playwright": "^1.50.0"
  }
}
