# Handoff — BQ-270 Recursive Prophet capture-first redesign

- **Owning instance:** Forge-Beta (Codex desktop)
- **Acting domain:** Patch
- **Branch:** `work/BQ-270`
- **Queue row:** RecursiveProphet.com — Capture-First Redesign and Release Hardening / BQ-270 / 3b066630-03ed-8195-a458-e5680a439ef4
- **Objective:** Implement the approved capture-first redesign locally, including BQ-262's site-side form and static-route contract, and verify it against the repository's release criteria without deploying.
- **Outcome state:** implementation complete; verification passed; ready for draft-PR review

- **Last code commit / Handoff base SHA:** `9dbacae` (claim commit; implementation commit pending this handoff update)
- **Files changed:** home-page components and hierarchy, shared visual system, capture configuration,
  three static support routes, measured design constraints, publishing-contract conflict note, and
  this handoff
- **Tests / lint run + results:** `npm ci` passed (210 packages, 0 vulnerabilities); `npm run build`
  passed with four static routes; `npm run audit -- --strict` passed at 1440×900 and 390×844 with
  CLS 0, no overflow, zero axe violations, correct responsive cover selection, and all six FAQ
  keyboard checks passing; generated-form contract checks passed; `git diff --check` passed.
  `npm run check` could not perform type analysis because the existing project does not declare
  `@astrojs/check` or `typescript`; no dependency change was added to this redesign.
- **Uncommitted state:** implementation and verification documentation pending scoped commit

- **Decisions made:** Extend BQ-258 rather than modify or reuse PR #3. Preserve static-first Astro,
  email-only no-JavaScript capture, native FAQ behavior, responsive cover negotiation, zero CLS,
  placeholder gating, and RP/SP separation. Use the human premise and Chapter 1 as the unambiguous
  conversion promise; preserve the First File versus Welcome to the Archive source conflict instead
  of guessing. Implement BQ-262's local form contract and requested/problem route shells; leave n8n,
  MailerLite, deployment, DNS, analytics activation, and definitive literary/legal copy outside this
  branch.
- **Known risks:** `build:prod` intentionally remains blocked by owner-controlled placeholders: the
  relay action, retailer links, approved cover master, and legal/consent source text. No public form
  target or launch promise was invented.
- **Exact next action:** Review the draft PR. Forge should cross-review the form/route contract;
  Vira/Doc should reconcile the lead-magnet payload; Rook should provide the frozen consent and
  privacy language. Prof can then decide whether to approve a separate production action.
- **Blockers requiring Prof:** final deployment and all R2/R3 actions; Doc final copy; approved cover master; Rook-approved legal/privacy text and destinations; production form/integration identifiers

- **Last updated:** 2026-08-02 by Forge-Beta (Codex desktop)
